server:
  port: 9002
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
    #    server-addr: localhost:8848 #配置Nacos地址
        server-addr: 192.168.81.128:1111 #Linux版Nacos+MySQL生产环境
management:
  endpoints:
    web:
      exposure:
        include: '*'
